<UserControl x:Class="MedicalManagementSystem.View.UserDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicalManagementSystem.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Row="0" Grid.Column="0" Margin="10">
                <Image Width="200" Source="../Images/profile.jpg"/>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0" Margin="10">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Codice Fiscale" Style="{StaticResource AddPanelLabel}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="3" VerticalContentAlignment="Center" CharacterCasing="Upper" Text="{Binding TextCodiceFiscale, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Nome" Style="{StaticResource AddPanelLabel}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="3" VerticalContentAlignment="Center" Text="{Binding TextNome}"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Cognome" Style="{StaticResource AddPanelLabel}"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="3" VerticalContentAlignment="Center" Text="{Binding TextCognome}"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Residenza" Style="{StaticResource AddPanelLabel}"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" Margin="3" IsEditable="True" ItemsSource="{Binding ResidenceList}" Text="{Binding TextResidenza}"/>
                    <!--<TextBox Grid.Row="4" Grid.Column="1" Margin="3" VerticalContentAlignment="Center"/>-->
                    <Label Grid.Row="4" Grid.Column="0" Content="Data di nascita" Style="{StaticResource AddPanelLabel}" Margin="6,7,0,0" VerticalAlignment="Top" Grid.RowSpan="2"/>
                    <DatePicker Grid.Row="4" Grid.Column="1" VerticalContentAlignment="Center" SelectedDate="{Binding SelectedDataDiNascita}"/>

                </Grid>
                
                <Grid Grid.Row="0" Grid.Column="1" Margin="10">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Sesso" Style="{StaticResource AddPanelLabel}"/>

                    <DockPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                        <RadioButton Foreground="#fff" Margin="2" GroupName="Sex" IsChecked="{Binding IsCheckedM}">M</RadioButton>
                        <RadioButton Foreground="#fff" Margin="2" GroupName="Sex" IsChecked="{Binding IsCheckedF}">F</RadioButton>
                    </DockPanel>


                    <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Content="Email" Foreground="#ffffff" Margin="6,3,0,3"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="3" VerticalContentAlignment="Center" Text="{Binding TextEmail}"/>

                    <Label Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Content="Telefono" Foreground="#ffffff" Margin="6,3,0,3"/>
                    <DockPanel Grid.Row="3" Grid.Column="1" >
                        <ComboBox Margin="3" Width="55" HorizontalContentAlignment="Center" Text="{Binding TextPrefisso}">
                            <ComboBoxItem Content="+39"/>
                            <ComboBoxItem Content="+44"/>
                            <ComboBoxItem Content="+35"/>
                        </ComboBox>
                        <TextBox Margin="3" VerticalContentAlignment="Center" Text="{Binding TextTelefono, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>


                </Grid>

            </Grid>

        </Grid>

        <Grid Grid.Row="1" Grid.Column="0">

        </Grid>

    </Grid>
</UserControl>
